<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Happy New Year!</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name"><h1><a href="#"><%= content_for?(:title)? yield(:title) : "Кто мой самый дорогой человек?" %></a></h1></li>
    </ul>
    <section class="top-bar-section">
      <!-- Right Nav Section -->

      <!-- Left Nav Section -->
      <ul class="left">
        <li class="has-form">
          <div class="row collapse">
            <%= render 'specials/form' %>
          </div>
        </li>
      </ul>
    </section>
  </nav>
  <header>
      <div class="row">
          <div class="large-6 large-offset-3 columns">
          <img src="<%= asset_path( 'New_Year_2014_Image.jpg' ) %>" /></div>
      </div>
    </header>
    <section class="main">
      <%= yield %>
    <%= javascript_include_tag "application" %>
    </section>

  </body>
</html>